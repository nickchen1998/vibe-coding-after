# Grafana 開機自動接上 Loki 資料源
#
# uid 固定寫死，儀表板與告警規則才能用同一個代號指向它——
# 否則每次重建容器都會拿到一個新的隨機 uid，版控裡的 JSON 就對不上了。
apiVersion: 1
datasources:
  - name: Loki
    type: loki
    uid: loki-ironman
    access: proxy
    url: http://loki:3100
    isDefault: true
    jsonData:
      maxLines: 1000
